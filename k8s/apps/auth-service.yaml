---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: epm-auth
  namespace: epm
  labels:
    app: epm-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epm-auth
  template:
    metadata:
      labels:
        app: epm-auth
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/erickjoelquispe/epm-auth:latest
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: epm-config
            - secretRef:
                name: epm-secrets
          resources:
            requests:
              cpu: 256m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: epm-auth
  namespace: epm
  labels:
    app: epm-auth
spec:
  type: ClusterIP
  selector:
    app: epm-auth
  ports:
    - name: http
      port: 8081
      targetPort: 8081
