---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: epm-discovery
  namespace: epm
  labels:
    app: epm-discovery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epm-discovery
  template:
    metadata:
      labels:
        app: epm-discovery
    spec:
      containers:
        - name: discovery-service
          image: ghcr.io/erickjoelquispe/epm-discovery:latest
          ports:
            - containerPort: 8761
          envFrom:
            - configMapRef:
                name: epm-config
            - secretRef:
                name: epm-secrets
          resources:
            requests:
              cpu: 256m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: epm-discovery
  namespace: epm
  labels:
    app: epm-discovery
spec:
  type: ClusterIP
  selector:
    app: epm-discovery
  ports:
    - name: http
      port: 8761
      targetPort: 8761
