---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: epm-task
  namespace: epm
  labels:
    app: epm-task
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epm-task
  template:
    metadata:
      labels:
        app: epm-task
    spec:
      containers:
        - name: task-service
          image: ghcr.io/erickjoelquispe/epm-task:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: epm-config
            - secretRef:
                name: epm-secrets
          resources:
            requests:
              cpu: 256m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: epm-task
  namespace: epm
  labels:
    app: epm-task
spec:
  type: ClusterIP
  selector:
    app: epm-task
  ports:
    - name: http
      port: 8084
      targetPort: 8084
