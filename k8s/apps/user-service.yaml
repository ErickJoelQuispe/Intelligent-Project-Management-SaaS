---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: epm-user
  namespace: epm
  labels:
    app: epm-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epm-user
  template:
    metadata:
      labels:
        app: epm-user
    spec:
      containers:
        - name: user-service
          image: ghcr.io/erickjoelquispe/epm-user:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: epm-config
            - secretRef:
                name: epm-secrets
          resources:
            requests:
              cpu: 256m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: epm-user
  namespace: epm
  labels:
    app: epm-user
spec:
  type: ClusterIP
  selector:
    app: epm-user
  ports:
    - name: http
      port: 8082
      targetPort: 8082
